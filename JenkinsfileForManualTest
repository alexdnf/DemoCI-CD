pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                // Запускаем Java-приложение
                bat 'java -jar ./artifacts/app-mbank.jar'
            }
        }
    }
}
